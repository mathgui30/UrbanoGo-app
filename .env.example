# Copy to .env before building (flutter_dotenv reads .env as a bundled asset).
# For a backend on the host machine via the Android emulator, set:
#   API_URL=http://10.0.2.2:3000
API_URL=https://187-127-62-64.sslip.io
